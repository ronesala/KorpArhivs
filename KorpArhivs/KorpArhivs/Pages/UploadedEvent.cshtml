@page "{category}/{id}"
@model KorpArhivs.Pages.UploadedEventModel
@{
}

<div class="p-3">
    <div class="row row-grid mt-3">
        <div class="col-10">
            <h3>@Model.EventName <i class="far fa-edit"></i></h3>
        </div>
        <div class="col-1 justify-content-end pb-2">
            <h4>@Model.EventDate.ToString("dd/MM/yyyy")</h4>
        </div>
    </div>
    <div class="pb-3">
        @foreach (var keyword in Model.Keywords)
        {
            <span class="badge rounded-pill bg-light text-dark">@keyword</span>
        }

    </div>
    <div class="pb-3 pt-3">
        <p>@Model.EventDescription</p>
    </div>
    <div class="pb-3">
        <div class="container">
            @if (Model.Category == "Bildes"){
            <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3">
                @foreach (var uploadedUri in Model.UploadedFiles)
                {
                    <div class="col mt-3">
                        <img src="@uploadedUri" class="img-fluid" alt="image">
                    </div>
                }
                </div>
            }
            else
            {
                @foreach (var uploadedUri in Model.UploadedFiles)
                {
                    <div class="col mt-3">
                        <a href="@uploadedUri" class="link-secondary" download>@uploadedUri.Split('/').Last()</a>
                    </div>
                }
            }
        </div>
    </div>
</div>